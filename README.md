# Zayen Clicker

A Vercel-ready idle clicker game.

## Run locally
```bash
npm install
npm run dev
```

## Deploy to Vercel
Upload/import this folder as a project. The build command is:

```bash
npm run build
```

The output directory is:

```bash
dist
```

Progress is saved in the browser with localStorage.
